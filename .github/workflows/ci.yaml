name: 'poc-php'
'on':
    push:
        branches:
            - master
            - develop
    pull_request: null
jobs:
    lint:
        strategy:
            fail-fast: false
            matrix:
                operating-system:
                    - ubuntu-latest
                php-versions:
                    - '7.3'
        runs-on: '${{ matrix.operating-system }}'
        name: 'Lint / PHP ${{ matrix.php-versions }} @ ${{ matrix.operating-system }}'
        steps:
          - name: 'Install dependencies with Composer'
            run: 'composer install --prefer-dist '

          - name: 'Linting tests files'
            run: 'php -l tests'

          - name: 'Linting '
            run: 'php -l'